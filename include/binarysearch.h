#ifndef BINARY_SEARCH_H
#define BINARY_SEARCH_H
int binary_search(int*, unsigned, unsigned, int);
int upper_bound(int*, unsigned, unsigned, int);
int lower_bound(int*, unsigned, unsigned, int);
#endif
